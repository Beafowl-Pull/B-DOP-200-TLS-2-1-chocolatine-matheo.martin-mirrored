name: Print the branch and fetch
on: [push]
jobs:
  Explore-Github-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: echo "💡 The ${GITHUB_REF_NAME} branch has been cloned to the runner."
      - run: du -skh --exclude=".git"
  